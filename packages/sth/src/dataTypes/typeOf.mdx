import { typeOf } from 'sth';

typeOf(2); // number
typeOf(true); // boolean

// 正确判断引用数据类型
typeof []; // object
typeOf([]); // array

typeof null; // object
typeOf(null); // null

typeof /a/; // object
typeOf(/a/); // regexp